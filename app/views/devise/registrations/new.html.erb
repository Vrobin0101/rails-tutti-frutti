<div class="container">
  <div class="m-5 d-grid justify-content-center background-sign-up" style="background-image: url(<%= image_url('ananas.svg') %>)">
    <h2 class="sign-in-title pb-3">Inscription</h2>

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: :false }) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :first_name,
                    autofocus: true,
                    label: false,
                    input_html: { id: 'input-registration' },
                    label_html: { class: 'label-sign-input'},
                    placeholder: 'Prénom' %>
        <%= f.input :last_name,
                    autofocus: true,
                    label: false,
                    input_html: { id: 'input-registration' },
                    label_html: { class: 'label-sign-input'},
                    placeholder: 'Nom' %>
        <%= f.input :username,
                    autofocus: true,
                    label: false,
                    input_html: { id: 'input-registration' },
                    label_html: { class: 'label-sign-input'},
                    placeholder: "Nom d'utilisateur" %>
        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    label: false,
                    input_html: { autocomplete: "email", id: 'input-registration' },
                    label_html: { class: 'label-sign-input'},
                    placeholder: 'michael@dundermifflin.com' %>
        <%= f.input :password,
                    required: true,
                    label: false,
                    hint: ("#{@minimum_password_length} lettres minimum" if @minimum_password_length),
                    input_html: { autocomplete: "new-password" },
                    label_html: { class: 'label-sign-input'},
                    placeholder: 'Mot de passe' %>
        <%= f.input :password_confirmation,
                    required: true,
                    label: false,
                    input_html: { autocomplete: "new-password", id: 'last-input-registration'  },
                    label_html: { class: 'label-sign-input'},
                    placeholder: 'Confirmation mot de passe' %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "S'inscrire", class: 'btn-info' %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
