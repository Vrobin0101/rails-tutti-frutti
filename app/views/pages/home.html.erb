<div class="container">
  <h2>Recherche ton fruit ou ton légume</h2>
  <%= simple_form_for :search, url: root_path, method: :get do |f| %>
    <div class="search-form-control form-group mb-3">
      <input class="form-control string required" type="text" name="search[query]" id="search_query" />
      <button name="button" type="submit" class="btn btn-flat">
        <i class="fas fa-search"></i>
      </button>
    </div>
  <% end %>
  <h3>Les fruits et legumes de <%= @current_month %></h3>

   <div class="swiper mySwiper mb-3" data-controller="swiper">
      <div class="swiper-wrapper">
      <% @products.each_with_index do |product, index| %>
        <div class="swiper-slide">
          <div class="card-category">
            <%= product.name %>
          </div>
        </div>
      <% end %>
    </div>
    <div class="swiper-pagination"></div>
  </div>
  <%# ---------------------  ------------- %>
  <div class="container border p-2 mb-3">
    <p>En mai, on fête l'arrivée des fruits rouges, allez-y sans modération !</p>
  </div>
  <h2 class="text-center">Tous les fruits et légumes du moment !</h2>
  <div class="d-flex justify-content-between mb-3">
    <div class="card-category w-100 mx-1 bg-warning">Fruits</div>
    <div class="card-category w-100 mx-1 bg-primary">Légumes</div>
  </div>
  <h2>Où les trouver ?</h2>
  <%# map here %>
  <%# ---------- %>
</div>
